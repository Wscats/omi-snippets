import {readFile} from 'fs';
; (() => {
    // console.log(fs);
    // const data:string = fs.readFileSync('./index.js').toString();
    // const a: number = 1;
    // (async () => {
    //     await new Promise((resolve, reject) => {
    //         setTimeout(() => { resolve }, 1000)
    //     })
    // })();
    console.log(readFile);
})()

